{
  "name": "gforms-proxy",
  "version": "1.0.0",
  "description": "Google Forms → JSON Schema proxy on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "cf-types": "wrangler types"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@google/genai": "^1.33.0",
    "@types/node": "^24.1.0",
    "typescript": "^5.9.3",
    "vitest": "^2.0.0",
    "wrangler": "^4.54.0"
  }
}
